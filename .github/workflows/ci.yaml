name: Demo CI

on:   
  push:
    # Publish `main` as Docker `latest` image.
    branches:
      - main

  # Run CI for any PRs.
  pull_request:

  # Run manually.
  workflow_dispatch:

jobs:
  build-image:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read

    steps:
      - uses: actions/checkout@v2

      - name: Set variables
        run: |
          version=manual-${GITHUB_RUN_ID}
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            version=pr-${{ github.event.number }}
          elif [[ "${{ github.event_name }}" == "push" ]]; then
            version=$(<VERSION)
          fi

          echo "VERSION=${version}" >> $GITHUB_ENV
          echo "REGISTRY=ghcr.io/${{ github.repository_owner }}" >> $GITHUB_ENV

      - name: Build image
        run: |
          make build

      - name: Log in to registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: |
          make push

  trigger-deployment:
    needs: build-image
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - uses: actions/checkout@v2
        with:
          repository: ${{ github.repository_owner }}/guestbook-go-config
          token: ${{ secrets.CONFIG_GITHUB_TOKEN }}

      - name: Setup argocd-util
        run: |
          curl -L -o argocd-util "https://github.com/argoproj/argo-cd/releases/download/v2.0.5/argocd-util-linux-amd64"
          chmod +x argocd-util

      - name: Create new env for PR
        run: |
          ./argocd-util app generate-spec guestbook-pr-${{ github.event.number }} \
            --project default \
            --repo https://github.com/${{ github.repository_owner }}/guestbook-go-config \
            --path overlays/pr \
            --dest-namespace guestbook-pr-${{ github.event.number }} \
            --dest-server https://kubernetes.default.svc \
            --sync-policy automated \
            --auto-prune \
            --self-heal \
            --sync-option CreateNamespace=true \
            --kustomize-image ghcr.io/${{ github.repository_owner }}/guestbook-go:pr-${{ github.event.number }} \
          > apps/guestbook-pr-${{ github.event.number }}.yaml

          git config user.email "guestbook-go-ci@example.com"
          git config user.name "guestbook-go CI"

          git add apps/guestbook-pr-${{ github.event.number }}.yaml
          git commit -m "Add new app for pr ${{ github.event.number }}"
          git push origin main